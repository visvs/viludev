# data/

Small static structures that behave like configuration: the skills taxonomy,
social links, navigation items, site metadata.

Belongs here if it is a short, stable list rather than something you author.
Long or translatable prose belongs in `content/`.
