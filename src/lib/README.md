# lib/

Pure, framework-agnostic functions. Nothing here may import React or Astro.

This is where most test coverage lives, because this is where the logic lives:
i18n helpers, SEO/JSON-LD builders, the analytics wrapper, `cn`.
